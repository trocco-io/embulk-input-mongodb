net:
  tls:
    mode: allowTLS
    certificateKeyFile: /etc/mongo/mongo.key
    CAFile: /etc/mongo/mongo.crt

security:
  authorization: enabled

setParameter:
  authenticationMechanisms: "SCRAM-SHA-256,MONGODB-X509"